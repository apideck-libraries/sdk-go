// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package operations

import (
	"github.com/apideck-libraries/sdk-go/internal/utils"
	"github.com/apideck-libraries/sdk-go/models/components"
	"io"
)

type ProxyDeleteProxyGlobals struct {
	// ID of the consumer which you want to get or push data from
	ConsumerID *string `header:"style=simple,explode=false,name=x-apideck-consumer-id"`
	// The ID of your Unify application
	AppID *string `header:"style=simple,explode=false,name=x-apideck-app-id"`
}

func (p *ProxyDeleteProxyGlobals) GetConsumerID() *string {
	if p == nil {
		return nil
	}
	return p.ConsumerID
}

func (p *ProxyDeleteProxyGlobals) GetAppID() *string {
	if p == nil {
		return nil
	}
	return p.AppID
}

type ProxyDeleteProxyRequest struct {
	// ID of the consumer which you want to get or push data from
	ConsumerID *string `header:"style=simple,explode=false,name=x-apideck-consumer-id"`
	// The ID of your Unify application
	AppID *string `header:"style=simple,explode=false,name=x-apideck-app-id"`
	// Provide the service id you want to call (e.g., pipedrive). Only needed when a consumer has activated multiple integrations for a Unified API.
	ServiceID string `header:"style=simple,explode=false,name=x-apideck-service-id"`
	// Specify which unified API to use for the connection lookup. Required for multi-API connectors (e.g., Workday) to ensure the correct credentials are used.
	UnifiedAPI *string `header:"style=simple,explode=false,name=x-apideck-unified-api"`
	// Downstream URL
	DownstreamURL string `header:"style=simple,explode=false,name=x-apideck-downstream-url"`
	// Downstream authorization header. This will skip the Vault token injection.
	DownstreamAuthorization *string `header:"style=simple,explode=false,name=x-apideck-downstream-authorization"`
	// Override the default downstream request timeout in milliseconds. The default is 28000 (28 seconds).
	Timeout *int64 `default:"28000" header:"style=simple,explode=false,name=x-apideck-timeout"`
	// Set to `false` to opt out of the redirect to the presigned download URL. Instead of a `30x` response, you receive a `200` JSON body `{ url, expires_at }` containing the URL and its expiry, which you can fetch explicitly. Use this if your client automatically forwards the `Authorization` header onto redirects, since the downstream storage provider will reject that request. Any value other than `false` (or omitting the header) preserves the default redirect behavior.
	FollowRedirects *bool `default:"true" header:"style=simple,explode=false,name=x-apideck-follow-redirects"`
}

func (p ProxyDeleteProxyRequest) MarshalJSON() ([]byte, error) {
	return utils.MarshalJSON(p, "", false)
}

func (p *ProxyDeleteProxyRequest) UnmarshalJSON(data []byte) error {
	if err := utils.UnmarshalJSON(data, &p, "", false, nil); err != nil {
		return err
	}
	return nil
}

func (p *ProxyDeleteProxyRequest) GetConsumerID() *string {
	if p == nil {
		return nil
	}
	return p.ConsumerID
}

func (p *ProxyDeleteProxyRequest) GetAppID() *string {
	if p == nil {
		return nil
	}
	return p.AppID
}

func (p *ProxyDeleteProxyRequest) GetServiceID() string {
	if p == nil {
		return ""
	}
	return p.ServiceID
}

func (p *ProxyDeleteProxyRequest) GetUnifiedAPI() *string {
	if p == nil {
		return nil
	}
	return p.UnifiedAPI
}

func (p *ProxyDeleteProxyRequest) GetDownstreamURL() string {
	if p == nil {
		return ""
	}
	return p.DownstreamURL
}

func (p *ProxyDeleteProxyRequest) GetDownstreamAuthorization() *string {
	if p == nil {
		return nil
	}
	return p.DownstreamAuthorization
}

func (p *ProxyDeleteProxyRequest) GetTimeout() *int64 {
	if p == nil {
		return nil
	}
	return p.Timeout
}

func (p *ProxyDeleteProxyRequest) GetFollowRedirects() *bool {
	if p == nil {
		return nil
	}
	return p.FollowRedirects
}

type ProxyDeleteProxyResponse struct {
	HTTPMeta components.HTTPMetadata `json:"-"`
	// Ok. When the request includes `x-apideck-follow-redirects: false` and the downstream response would otherwise redirect to a presigned URL (oversized responses), the body is instead an `application/json` object `{ url, expires_at }` — fetch `url` explicitly (without the Authorization header) to retrieve the payload.
	ResponseJSON map[string]any
	// Ok. When the request includes `x-apideck-follow-redirects: false` and the downstream response would otherwise redirect to a presigned URL (oversized responses), the body is instead an `application/json` object `{ url, expires_at }` — fetch `url` explicitly (without the Authorization header) to retrieve the payload.
	// The Close method must be called on this field, even if it is not used, to prevent resource leaks.
	ResponseBinary io.ReadCloser
	// Ok. When the request includes `x-apideck-follow-redirects: false` and the downstream response would otherwise redirect to a presigned URL (oversized responses), the body is instead an `application/json` object `{ url, expires_at }` — fetch `url` explicitly (without the Authorization header) to retrieve the payload.
	// The Close method must be called on this field, even if it is not used, to prevent resource leaks.
	ResponsePdf io.ReadCloser
	// Ok. When the request includes `x-apideck-follow-redirects: false` and the downstream response would otherwise redirect to a presigned URL (oversized responses), the body is instead an `application/json` object `{ url, expires_at }` — fetch `url` explicitly (without the Authorization header) to retrieve the payload.
	ResponseXML *string
	// Ok. When the request includes `x-apideck-follow-redirects: false` and the downstream response would otherwise redirect to a presigned URL (oversized responses), the body is instead an `application/json` object `{ url, expires_at }` — fetch `url` explicitly (without the Authorization header) to retrieve the payload.
	ResponseCsv *string
	// Ok. When the request includes `x-apideck-follow-redirects: false` and the downstream response would otherwise redirect to a presigned URL (oversized responses), the body is instead an `application/json` object `{ url, expires_at }` — fetch `url` explicitly (without the Authorization header) to retrieve the payload.
	ResponseText *string
	// Proxy error
	ErrorJSON map[string]any
	// Proxy error
	ErrorXML *string
	// Proxy error
	ErrorHTML *string
	// Proxy error
	ErrorText *string
	Headers   map[string][]string
}

func (p *ProxyDeleteProxyResponse) GetHTTPMeta() components.HTTPMetadata {
	if p == nil {
		return components.HTTPMetadata{}
	}
	return p.HTTPMeta
}

func (p *ProxyDeleteProxyResponse) GetResponseJSON() map[string]any {
	if p == nil {
		return nil
	}
	return p.ResponseJSON
}

func (p *ProxyDeleteProxyResponse) GetResponseBinary() io.ReadCloser {
	if p == nil {
		return nil
	}
	return p.ResponseBinary
}

func (p *ProxyDeleteProxyResponse) GetResponsePdf() io.ReadCloser {
	if p == nil {
		return nil
	}
	return p.ResponsePdf
}

func (p *ProxyDeleteProxyResponse) GetResponseXML() *string {
	if p == nil {
		return nil
	}
	return p.ResponseXML
}

func (p *ProxyDeleteProxyResponse) GetResponseCsv() *string {
	if p == nil {
		return nil
	}
	return p.ResponseCsv
}

func (p *ProxyDeleteProxyResponse) GetResponseText() *string {
	if p == nil {
		return nil
	}
	return p.ResponseText
}

func (p *ProxyDeleteProxyResponse) GetErrorJSON() map[string]any {
	if p == nil {
		return nil
	}
	return p.ErrorJSON
}

func (p *ProxyDeleteProxyResponse) GetErrorXML() *string {
	if p == nil {
		return nil
	}
	return p.ErrorXML
}

func (p *ProxyDeleteProxyResponse) GetErrorHTML() *string {
	if p == nil {
		return nil
	}
	return p.ErrorHTML
}

func (p *ProxyDeleteProxyResponse) GetErrorText() *string {
	if p == nil {
		return nil
	}
	return p.ErrorText
}

func (p *ProxyDeleteProxyResponse) GetHeaders() map[string][]string {
	if p == nil {
		return map[string][]string{}
	}
	return p.Headers
}
