# CompaniesSortBy

The field on which to sort the Companies

## Example Usage

```go
import (
	"github.com/apideck-libraries/sdk-go/models/components"
)

value := components.CompaniesSortByCreatedAt

// Open enum: custom values can be created with a direct type cast
custom := components.CompaniesSortBy("custom_value")
```


## Values

| Name                       | Value                      |
| -------------------------- | -------------------------- |
| `CompaniesSortByCreatedAt` | created_at                 |
| `CompaniesSortByUpdatedAt` | updated_at                 |
| `CompaniesSortByName`      | name                       |